# Native ServiceNow incident-platform configuration
#
# This file is guidance only; the connection is authorized and saved in the
# Azure SRE Agent portal. It is not an MCP server or a credential manifest.
#
# Instance:
#   https://dev442167.service-now.com
#
# Source:
#   Native ServiceNow incident ingestion, filtered to the Zava Power SRE
#   assignment group.
#
# Lifecycle tools:
#   GetServiceNowIncident
#   AcknowledgeServiceNowIncident
#   PostServiceNowDiscussionEntry
#   ResolveServiceNowIncident
#
# Identifier:
#   Pass the triggering incident sys_id to every native action. INC numbers are
#   display-only.
#
# Authentication:
#   Use the mode exposed by the SRE Agent tenant. Keep simulator OAuth client
#   credentials in local environment variables; never store them here.
#
# Setup:
#   See docs/SERVICENOW-SETUP.md.
